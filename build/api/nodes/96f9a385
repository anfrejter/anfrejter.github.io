{
  "code": "def plot_mood_state_distribution(data: pd.DataFrame):\n    \"\"\"\n    Plot the distribution of the target variable 'mood'\n    \"\"\"\n    return (data.groupby('Mood_State', observed=False)\n            .count()[['Time']]\n            .reset_index()\n            .rename(columns={'Time':'Count'}))\n",
  "filepath": "anfrejter.github.io/src/psychological_state_classification/pipelines/eda/nodes.py",
  "parameters": {},
  "run_command": "kedro run --to-nodes='plotting1.plot_mood_state_distribution'",
  "inputs": [
    "preprocessed_data"
  ],
  "outputs": [
    "plot_mood_state_distribution_px"
  ]
}